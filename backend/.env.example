# Environment Configuration Example
# Copy this to .env and update values

# Application Settings
NODE_ENV=development
PORT=3000

# Database Configuration
DATABASE_URL=postgresql://admin:admin@postgres:5432/postgres

# Security Settings
JWT_SECRET=dev-jwt-secret-change-for-production-use-32-chars-minimum

# CORS and Frontend Settings
CORS_ORIGIN=http://localhost:3001
FRONTEND_URL=http://localhost:3001

# Email Configuration (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=internal.software@dreamteameng.org
SMTP_PASSWORD=

# For Production, change these:
# NODE_ENV=production
# DATABASE_URL=postgresql://admin:SECURE_PASSWORD@your-db-host:5432/postgres
# JWT_SECRET=your-secure-jwt-secret-32-characters-minimum
# CORS_ORIGIN=https://your-frontend-domain.com
# FRONTEND_URL=https://your-frontend-domain.com
# SMTP_PASSWORD=your-email-app-password
EOF < /dev/null